
let btns=document.querySelectorAll(".calc-button");
let display=document.querySelector(".display");
let cc=0

btns.forEach(button => {
    button.addEventListener('click', (event) => {
        const buttonText = event.target.textContent; 
        console.log(buttonText);
    
  });}
);
